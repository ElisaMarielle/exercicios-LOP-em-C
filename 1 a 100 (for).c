#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(){
    //variáveis
    int i
    
    //escreve 1 numero ou alguma coisa até um total pré determinado
    //for: (inicialização; condição; incremento) >(1 (inicia no 1); <=100(repete enquanto o número é menor que 100))
    for(i=1;i<=100;i++){
        printf("%d ",i);
    }
    
    return 0;
}